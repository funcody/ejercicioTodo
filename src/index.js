//----------- CSS -----------// Aquí irán todos los estilos que se necesiten para este archivoimport './styles.css';//----------- CLASES -----------// Desde aquí solo importamos el archivo genérico que contiene todas las referencias de las clases.// Cuando especificamos solo el directorio y no le especificamos el archivo, Javascript busca por defecto el index.js// Dentro de las llaves añadimos todas las clases que queramos importar del archivo index.js (del directorio classes)import { TodoList, } from './classes';import { crearTodoHtml, insertarNumeroTodosPendientes } from './js/componentes.js'// -------------------------------//----------- COMIENZO -----------// -------------------------------//----------- INSTANCIAS -----------// Desde aquí insertaremos las instancias necesarias.// Instancia del tudu listexport const todoList = new TodoList();// Esta linea es la que ejecuta el programa.// Llama todoList._todos, que son todos los tudus que hay el array guardado de esta clase.// Al llamarlos le estamos pasando a cada elemento una función que le añade código html y posiciona los elementos de las instancias.// cargarLocalStorage() se está cargando desde el constructor de todoList.todoList._todos.forEach(todo => crearTodoHtml(todo));insertarNumeroTodosPendientes();//const nuevoTodo = new Todo ('Todo de prueba duro')//todoList.nuevoTodo(nuevoTodo);//crearTodoHtml (nuevoTodo);console.log(todoList);// Como truco se podría quedar la función más reducida.// Cuando tenemos una instrucción como la de arriba, donde el argumento que queremos enviar, es el único argumento que enviamos// A otra función o método, entonces podemos obviar poner el nombre indicativo del argumento, y el argumento enviado.// Está queriendo decir que el primer argumento del callback que estamos teniendo en el foreach o en cualquier método (filter, find, map...)// está llamando al crear la función que estemos especificando dentro de los paréntesis y el argumento que estaríamos enviando dentro de la función// son los elementos que está enviando el foreach.// Sería así:// todoList._todos.forEach(crearTodoHtml);  --> ESTO SOLO FUNCIONA SI SOLO SE ENVÍA UN SOLO ARGUMENTO DESDE LA FUNCIÓN INTERNA (crearTodoHtml). Si enviara dos no funciona.